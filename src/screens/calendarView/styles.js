import { StyleSheet } from "react-native";
import { windowBackground, textColor, bgColor } from "../../assets/colors";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: bgColor ,
  },
});
